{
  "name": "@tinybox/release",
  "version": "1.0.1",
  "description": "",
  "main": "./bin/index.js",
  "bin": "./bin/index.js",
  "author": "Juan Martin Muda",
  "license": "MIT",
  "scripts": {
    "postversion": "npx @tinybox/changelog && git add CHANGELOG.md && git commit -m \"Bumped version\" && git push --follow-tags"
  }
}
